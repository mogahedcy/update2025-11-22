# دليل إعداد نظام الجدولة التلقائية

## ✅ ما تم إنجازه

تم تطوير نظام جدولة كامل يشمل:

1. **قاعدة البيانات:**
   - جدول `automation_schedules` - لحفظ إعدادات الجدولة
   - جدول `automation_logs` - لتتبع المهام المنفذة

2. **API Endpoints:**
   - `GET/POST /api/automation/schedule` - حفظ/قراءة إعدادات الجدولة
   - `GET /api/cron/scheduled-tasks` - تنفيذ المهام المجدولة
   - `GET /api/automation/logs` - عرض سجل المهام

3. **الواجهة:**
   - تبويبة الجدولة محدثة بالكامل
   - حقل لإدخال المجال (niche) للتوليد الذكي
   - خيار النشر التلقائي
   - عرض سجل المهام المنفذة

## 🔧 خطوات التفعيل

### الخطوة 1: تطبيق التغييرات على قاعدة البيانات

بعد التأكد من توفر قاعدة البيانات (DATABASE_URL)، قم بتطبيق التغييرات:

```bash
bunx prisma db push
```

أو إذا كنت تفضل Migrations:

```bash
bunx prisma migrate dev --name add_automation_scheduling
```

### الخطوة 2: تعيين CRON_SECRET

أضف مفتاح سري قوي لحماية endpoint الـ Cron في ملف `.env`:

```bash
CRON_SECRET=your-very-secret-and-random-key-here
```

**مهم:** استخدم مفتاح عشوائي قوي (مثلاً 32 حرف). يمكنك توليده بهذا الأمر:

```bash
openssl rand -hex 32
```

### الخطوة 3: إعداد Cron Job

#### على Replit:
لا يدعم Replit Cron Jobs مباشرة، لكن يمكنك استخدام خدمة خارجية مثل:

**1. cron-job.org (مجاني)**
- انتقل إلى: https://cron-job.org/
- أنشئ حساباً
- أضف Cron Job جديد:
  - URL: `https://yoursite.com/api/cron/scheduled-tasks`
  - Method: GET
  - Headers: `Authorization: Bearer YOUR_CRON_SECRET`
  - Schedule: اختر التوقيت (مثلاً: كل ساعة، كل يوم في 2 صباحاً)

**2. EasyCron (مجاني)**
- انتقل إلى: https://www.easycron.com/
- أنشئ حساباً
- أضف Cron Job مع نفس الإعدادات أعلاه

#### على Vercel:
إذا نشرت الموقع على Vercel، يمكنك استخدام Vercel Cron:

في ملف `vercel.json`:
```json
{
  "crons": [{
    "path": "/api/cron/scheduled-tasks",
    "schedule": "0 2 * * *"
  }]
}
```

#### على خادم خاص (VPS):
أضف في crontab:
```bash
0 2 * * * curl -H "Authorization: Bearer YOUR_CRON_SECRET" https://yoursite.com/api/cron/scheduled-tasks
```

## 📊 كيفية الاستخدام

### 1. إعداد الجدولة من لوحة التحكم

1. افتح: `/dashboard/automation`
2. اذهب إلى تبويبة **"الجدولة"**
3. فعّل **"التوليد الذكي التلقائي"**:
   - أدخل المجال (مثال: "برجولات خشبية")
   - حدد التكرار (يومي/أسبوعي/شهري)
   - حدد عدد المقالات (1-10)
   - اختر النشر التلقائي (اختياري)
4. فعّل **"إصلاح SEO التلقائي"** (اختياري):
   - حدد التكرار
5. اضغط **"حفظ الإعدادات"**

### 2. مراقبة التنفيذ

- سيظهر سجل المهام المنفذة في نفس الصفحة
- لكل مهمة ستجد:
  - نوع المهمة (توليد مقالات / إصلاح SEO)
  - الحالة (نجح/فشل)
  - عدد النجاح/الفشل
  - تاريخ ووقت التنفيذ

## 🔐 الأمان

1. **CRON_SECRET**: لا تشارك هذا المفتاح مع أحد
2. **Headers**: تأكد من إرسال Authorization header في كل طلب
3. **HTTPS**: استخدم HTTPS دائماً في الإنتاج

## 🐛 استكشاف الأخطاء

### المهام لا تُنفذ؟
1. تحقق من أن CRON_SECRET محدد بشكل صحيح
2. تحقق من أن Cron Job يعمل (راجع logs الخدمة الخارجية)
3. راجع سجل المهام في لوحة التحكم

### رسالة "CRON_SECRET مفقود"؟
- تأكد من إضافة `CRON_SECRET=...` في ملف `.env`
- أعد تشغيل الخادم بعد الإضافة

### خطأ في قاعدة البيانات؟
- تأكد من تطبيق migrations: `bunx prisma db push`
- تحقق من أن DATABASE_URL صحيح

## 📝 ملاحظات

- التوقيت المحدد (2 صباحاً) يمكن تعديله في الكود أو Cron Job
- يمكن تشغيل المهام يدوياً باستدعاء `/api/cron/scheduled-tasks` مع Authorization header
- السجل يحفظ آخر 20 مهمة فقط (قابل للتعديل)

## 🎉 جاهز!

بعد إكمال الخطوات أعلاه، سيعمل نظام الجدولة تلقائياً ويولد المقالات ويصلح SEO حسب الجدول المحدد!
