# وكيل الذكاء الاصطناعي لتحسين SEO

## نظرة عامة

نظام متقدم مدعوم بـ OpenAI GPT-5 لتحليل وتحسين محركات البحث (SEO) بطريقة ذكية وفعالة. يوفر أدوات شاملة لتحليل المحتوى، توليد محتوى محسّن، تحليل المنافسين، واقتراح استراتيجيات تحسين قوية.

## الميزات الرئيسية

### 1. تحليل الكلمات المفتاحية (Keyword Analysis)
- استخراج الكلمات المفتاحية الأساسية والثانوية
- تحليل كثافة الكلمات المفتاحية
- تحديد نية البحث (معلوماتي، تجاري، معاملات)
- تقييم صعوبة المنافسة وفرص الترتيب
- كلمات طويلة محددة (Long-tail keywords)

**API Endpoint:** `POST /api/seo-agent/analyze-keywords`

**Request Body:**
```json
{
  "content": "المحتوى المراد تحليله",
  "targetKeywords": ["برجولات", "مظلات", "جدة"],
  "language": "ar"
}
```

**Response:**
```json
{
  "success": true,
  "data": {
    "primary_keywords": ["برجولات خشبية", "مظلات حدائق"],
    "secondary_keywords": ["تركيب برجولات", "تصميم مظلات"],
    "long_tail_keywords": ["أفضل شركة برجولات في جدة"],
    "keyword_density": { "برجولات": 2.5, "مظلات": 1.8 },
    "search_intent": "تجاري",
    "difficulty_score": 65,
    "opportunity_score": 78
  }
}
```

### 2. تحليل المحتوى (Content Analysis)
- درجة SEO الإجمالية (1-100)
- درجة سهولة القراءة
- تقييم استخدام الكلمات المفتاحية
- اكتشاف فجوات المحتوى
- اقتراحات تحسين محددة
- توليد meta tags محسّنة

**API Endpoint:** `POST /api/seo-agent/analyze-content`

**Request Body:**
```json
{
  "content": "محتوى الصفحة أو المقال",
  "targetKeywords": ["برجولات", "مظلات"],
  "url": "https://example.com/page"
}
```

**Response:**
```json
{
  "success": true,
  "data": {
    "seo_score": 82,
    "readability_score": 75,
    "keyword_optimization": "جيد - الكلمات المفتاحية موزعة بشكل طبيعي",
    "content_gaps": ["إضافة معلومات عن الأسعار", "ذكر أمثلة عملية"],
    "suggestions": ["إضافة عناوين فرعية H2", "تحسين الفقرة الافتتاحية"],
    "meta_title_suggestion": "أفضل برجولات ومظلات خشبية في جدة | محترفين الديار",
    "meta_description_suggestion": "نقدم أفضل خدمات تركيب البرجولات والمظلات...",
    "h1_suggestions": ["برجولات خشبية عالية الجودة في جدة"],
    "internal_linking_opportunities": ["ربط بصفحة الخدمات", "ربط بمشاريع سابقة"]
  }
}
```

### 3. توليد محتوى محسّن (Content Generation)
- كتابة مقالات SEO احترافية
- توليد أوصاف مشاريع جذابة
- إنشاء صفحات خدمات محسّنة
- محتوى أصلي 100% غير منسوخ
- استخدام طبيعي للكلمات المفتاحية

**API Endpoint:** `POST /api/seo-agent/generate-content`

**Request Body:**
```json
{
  "topic": "أفضل أنواع البرجولات الخشبية في جدة",
  "keywords": ["برجولات خشبية", "برجولات جدة", "تركيب برجولات"],
  "contentType": "article",
  "wordCount": 800
}
```

**Content Types:**
- `article`: مقال
- `project_description`: وصف مشروع
- `service_page`: صفحة خدمة

**Response:**
```json
{
  "success": true,
  "data": {
    "title": "أفضل أنواع البرجولات الخشبية في جدة لعام 2025",
    "content": "<h2>مقدمة</h2><p>...</p>",
    "meta_description": "اكتشف أفضل أنواع البرجولات الخشبية...",
    "tags": ["برجولات", "مظلات خشبية", "جدة", "حدائق"]
  }
}
```

### 4. تحليل المنافسين (Competitor Analysis)
- استخراج الكلمات المفتاحية المحتملة للمنافسين
- تحليل استراتيجية المحتوى
- اكتشاف فجوات المحتوى للاستفادة منها
- فرص الحصول على روابط خلفية
- مجالات يمكن التفوق فيها

**API Endpoint:** `POST /api/seo-agent/analyze-competitor`

**Request Body:**
```json
{
  "competitorUrl": "https://competitor.com",
  "industry": "البناء والإنشاءات"
}
```

### 5. اقتراح روابط داخلية ذكية (Internal Linking)
- تحليل المحتوى لإيجاد فرص الربط
- اختيار أنسب الصفحات للربط
- توليد نص رابط (anchor text) طبيعي
- تقييم درجة الملاءمة
- اقتراح موقع إدراج الرابط

**API Endpoint:** `POST /api/seo-agent/suggest-internal-links`

**Request Body:**
```json
{
  "content": "محتوى الصفحة",
  "availablePages": [
    {
      "title": "خدمات البرجولات",
      "url": "/services/pergolas",
      "keywords": ["برجولات", "تركيب"]
    }
  ]
}
```

### 6. توليد Meta Tags
- عناوين محسّنة (50-60 حرف)
- أوصاف جذابة (150-160 حرف)
- Open Graph tags
- Twitter Card tags
- تحسين نسبة النقر (CTR)

**API Endpoint:** `POST /api/seo-agent/generate-meta-tags`

### 7. تجميع الكلمات المفتاحية (Keyword Clustering)
- تجميع الكلمات المتشابهة
- تحديد نية البحث لكل مجموعة
- تقييم الأولوية
- اقتراح أفكار محتوى لكل مجموعة

**API Endpoint:** `POST /api/seo-agent/cluster-keywords`

**Request Body:**
```json
{
  "keywords": ["برجولات خشبية", "برجولات حديدية", "مظلات سيارات", "تنسيق حدائق"]
}
```

**Response:**
```json
{
  "success": true,
  "data": {
    "clusters": [
      {
        "cluster_name": "البرجولات",
        "keywords": ["برجولات خشبية", "برجولات حديدية"],
        "search_intent": "تجاري",
        "priority_score": 85,
        "content_ideas": [
          "مقارنة بين البرجولات الخشبية والحديدية",
          "دليل اختيار البرجولات المناسبة"
        ]
      }
    ]
  }
}
```

## لوحة التحكم (Dashboard)

يمكن الوصول إلى لوحة تحكم SEO Agent من خلال:

```
/dashboard/seo-agent
```

### الواجهة تشمل:
- 📊 تحليل المحتوى
- ✨ توليد محتوى محسّن
- 🎯 تحليل المنافسين
- 🔗 اقتراح روابط داخلية
- 🏷️ تجميع الكلمات المفتاحية

## التقنيات المستخدمة

- **OpenAI GPT-5**: أحدث نموذج من OpenAI
- **Next.js 15**: API Routes
- **TypeScript**: للأمان النوعي
- **Prisma**: لجلب البيانات من قاعدة البيانات

## الأمان

- جميع endpoints محمية بـ authentication
- التحقق من صحة البيانات باستخدام Zod
- معالجة الأخطاء بشكل آمن
- عدم تسريب أي معلومات حساسة

## المتطلبات

يجب توفير `OPENAI_API_KEY` في متغيرات البيئة:

```env
OPENAI_API_KEY=sk-...
```

## أمثلة الاستخدام

### مثال 1: تحليل مقال موجود

```typescript
const response = await fetch('/api/seo-agent/analyze-content', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    content: articleContent,
    targetKeywords: ['برجولات', 'مظلات', 'جدة']
  })
});

const { data } = await response.json();
console.log('SEO Score:', data.seo_score);
console.log('Suggestions:', data.suggestions);
```

### مثال 2: توليد محتوى جديد

```typescript
const response = await fetch('/api/seo-agent/generate-content', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    topic: 'أفضل مظلات السيارات في جدة',
    keywords: ['مظلات سيارات', 'مظلات جدة'],
    contentType: 'article',
    wordCount: 1000
  })
});

const { data } = await response.json();
// استخدم data.title و data.content
```

## أفضل الممارسات

1. **تحليل قبل التوليد**: قم بتحليل المحتوى الحالي قبل توليد محتوى جديد
2. **استخدم الكلمات المفتاحية بطريقة طبيعية**: لا تفرط في استخدامها
3. **راجع النتائج**: دائماً راجع المحتوى المولد قبل النشر
4. **استفد من تحليل المنافسين**: لاكتشاف فرص جديدة
5. **طبق الروابط الداخلية**: لتحسين بنية الموقع

## الدعم

للمساعدة والدعم، راجع الوثائق أو تواصل مع فريق التطوير.

---

© 2025 محترفين الديار العالمية - جميع الحقوق محفوظة
